export interface Products {
  id: number;
  description: string;
  status: boolean;
  title: string;
  smallDescription: string;
}

export interface Categories {
    id: number;
    title: string;
    
}

