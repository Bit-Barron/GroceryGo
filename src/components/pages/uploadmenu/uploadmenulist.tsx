import React from "react";

interface UploadMenuListProps {}

export const UploadMenuList: React.FC<UploadMenuListProps> = ({}) => {
  return (
    <div>
      <h1>Upload Menu List</h1>
    </div>
  );
};
